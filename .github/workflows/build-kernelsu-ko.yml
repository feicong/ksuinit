name: Build KernelSU kernel modules

on:
  workflow_dispatch:
    inputs:
      kernelsu_tag:
        description: 'KernelSU release tag or commit'
        required: false
        default: 'main'
        type: string

jobs:
  build-kernelsu-ko:
    name: Build kernelsu.ko for ${{ matrix.kmi }}
    uses: ./.github/workflows/ddk-lkm.yml
    strategy:
      fail-fast: false
      matrix:
        kmi:
          - android12-5.10
          - android13-5.10
          - android13-5.15
          - android14-5.15
          - android14-6.1
          - android15-6.6
          - android16-6.12
    with:
      kmi: ${{ matrix.kmi }}
      ddk_release: '20250825'
      kernelsu_tag: ${{ github.event.inputs.kernelsu_tag || 'main' }}

  collect-artifacts:
    name: Collect all artifacts
    runs-on: ubuntu-latest
    needs: build-kernelsu-ko
    if: always()

    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts

    - name: List built modules
      run: |
        echo "=== Built KernelSU Modules ==="
        find artifacts -name "*.ko" -type f | while read ko_file; do
          echo "📦 $(basename "$ko_file") ($(du -h "$ko_file" | cut -f1))"
        done
        
        echo ""
        echo "=== Build Matrix Results ==="
        total_expected=7
        total_found=$(find artifacts -name "*.ko" -type f | wc -l)
        echo "Expected: $total_expected modules"
        echo "Built: $total_found modules"
        
        if [ $total_found -eq $total_expected ]; then
          echo "✅ All KMI variants built successfully!"
        else
          echo "⚠️  Some builds may have failed"
        fi
